# Godot Project Audit

Generated by `tools/audit_godot_project.py`.

## Inventory
- `.gd`: 41
- `.tscn`: 34
- `.tres/.res`: 5
- `.godot`: 1
- Total scanned resources: 81

## Missing `res://` References
- None detected.

## Scene Script Issues
- None detected.

## Duplicate `class_name`
- None detected.

## Risky Generic `:=` Inference
- None detected.

## Possible Missing Signal Methods
- None detected.

## Possible Missing `$NodePath` References In Scenes
- None detected.

## Scene Transition Dependency Risks
- None detected.

## Notes
- This is a static audit. Godot CLI validation remains authoritative for parser/import errors.
- Signal and `$NodePath` checks are conservative and may include false positives.

## Scene Cycle Fix 2026-05-17
- Root cause confirmed: transition destinations were serialized as `PackedScene` dependencies and `res://` scene strings, causing Godot to scan Hub, Dungeon and Frontier while opening `MainMenu.tscn`.
- `AreaTransition3D.gd`, `DungeonEntrance3D.gd` and `ArenaPortal3D.gd` now use plain `target_scene_path: String`.
- Runtime scene changes normalize paths to `res://` immediately before `change_scene_to_file`.
- Hub transition destinations were changed from `target_scene = ExtResource(...)` to plain `target_scene_path`.
- Dungeon and Frontier return paths use plain strings without `res://` in scene files.
- `MainMenu.gd` no longer preloads the Hub; it uses `change_scene_to_file`.
- Static audit now reports scene transition dependency risks.

## Required Grep Results
- `git grep -n "target_scene = ExtResource" godot`: no matches.
- `git grep -n "@export var target_scene: PackedScene" godot`: no matches.
- `git grep -n "change_scene_to_packed(target_scene)" godot`: no matches.
- `git grep -n "res://scenes/hub/Hub_VeyrholdOutskirts.tscn" godot/scenes`: no matches.
- `git grep -n "res://scenes/world/FronteiraCinzas_PrototypeArea.tscn" godot/scenes`: no matches.
- `git grep -n "res://scenes/dungeons/MiniDungeon_CryptOfVeyrfall.tscn" godot/scenes`: no matches.

## Validation Results
- `godot --headless --path godot --quit`: pass.
- `godot --headless --path godot --scene res://scenes/ui/MainMenu.tscn --quit`: pass.
- `godot --headless --path godot --scene res://scenes/hub/Hub_VeyrholdOutskirts.tscn --quit`: pass.
- `godot --headless --path godot --scene res://scenes/world/FronteiraCinzas_PrototypeArea.tscn --quit`: pass.
- `godot --headless --path godot --scene res://scenes/dungeons/MiniDungeon_CryptOfVeyrfall.tscn --quit`: exits 0, with Godot RID/resource cleanup noise on shutdown after instantiating 3D bodies.
- `godot --path godot --editor --scene res://scenes/ui/MainMenu.tscn --quit-after 120`: exits 0 with no dependency dialog output.
